<!DOCTYPE html>
<html>
  <body>
    <h1><%= @article.title %></h1>
    <p><%= @article.content %></p>

    <form action="/articles/<%=@article.id %>" method="POST">
      <button type="submit" value="submit">Delete</button>
    </form>
  </body>
</html>
